@model IEnumerable<ICE_Server.Models.Device>

@{
    ViewBag.Title = "Index";
}
<div class="row">
    <hr class="hr-primary" />
    <ol class="breadcrumb bread-primary ">
        <li>@Html.ActionLink("Home", "Index", "Home")</li>
        <li>Device overview</li>
    </ol>
</div>
<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default panel-info">
            <div class="panel-heading">
                <h3 class="panel-title">All devices</h3>
            </div>
            <div class="panel-body">
                <table class="table" id="devices">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.ID)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.DeviceID)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.DeviceOS)
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    @foreach (var item in Model)
                    {
                        <tbody>
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ID)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DeviceID)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DeviceOS)
                                </td>
                                <td class="rightText">
                                    <a href="@Url.Action("Details", "Devices", new { id = item.ID })"><span class="glyphicon glyphicon-eye-open"></span></a>
                                    <a href="@Url.Action("Edit", "Devices", new { id = item.ID })"><span class="glyphicon glyphicon-pencil"></span></a>
                                    <a href="@Url.Action("Delete", "Devices", new { id = item.ID })"><span class="glyphicon glyphicon-trash"></span></a>

                                </td>
                            </tr>
                        </tbody>
                    }
                </table>
            </div>
            <div class="panel-footer rightText">
                <input type="button" value="Create new device" class="btn btn-primary" onclick="location.href='@Url.Action("Create", "Devices")'" />
            </div>
        </div>
    </div>
</div>