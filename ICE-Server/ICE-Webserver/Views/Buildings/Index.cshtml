@model IEnumerable<ICE_Server.Models.Building>

@{
    ViewBag.Title = "Index";
}

<div class="row">
    <hr class="hr-primary" />
    <ol class="breadcrumb bread-primary ">
        <li>@Html.ActionLink("Home", "Index", "Home")</li>
        <li>Building overview</li>
    </ol>
</div>
<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default panel-info">
            <div class="panel-heading">
                <h3 class="panel-title">All buildings</h3>
            </div>
            <div class="panel-body">
                <table class="table" id="overview">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.ID)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Name)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Location)
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    @foreach (var item in Model)
                    {
                        <tbody>
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ID)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Location)
                                </td>
                                <td class="rightText">
                                    <a href="@Url.Action("Details", "Buildings", new { id = item.ID })"><span class="glyphicon glyphicon-eye-open"></span></a>
                                    <a href="@Url.Action("Edit", "Buildings", new { id = item.ID })"><span class="glyphicon glyphicon-pencil"></span></a>
                                    <a href="@Url.Action("Delete", "Buildings", new { id = item.ID })"><span class="glyphicon glyphicon-trash"></span></a>

                                </td>
                            </tr>
                        </tbody>
                    }

                </table>
            </div>
            <div class="panel-footer rightText">
                <input type="button" value="Create new building" class="btn btn-primary" onclick="location.href='@Url.Action("Create", "Buildings")'" />
            </div>
        </div>
    </div>
</div>